"""Reserved for the FastAPI dashboard, which arrives after the MVP.

Intentionally empty: the layered layout is fixed up front so later code lands in
its layer rather than moving there. See docs/DESIGN.md §8.
"""
